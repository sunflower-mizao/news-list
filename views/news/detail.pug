doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title Document
    style.
      .contain{
      width: 600px;
      margin: 0 auto;
      }
      h1{
      text-align: center;
      }
  body
    .contain
      h1.title #{detailData.title}
      span #{detailData.from}
      time #{detailData.newTime}
      .content #{detailData.title}
